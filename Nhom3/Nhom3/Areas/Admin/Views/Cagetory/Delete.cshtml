@model Nhom3.Model.DANHMUC

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

@if (ViewBag.Error != null)
{
    <br />
    <div class="alert alert-danger" role="alert">@ViewBag.Error</div>}
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header" style="text-align:left;">Xóa danh mục</h1>
    </div>
</div>
<h3>Bạn có chắc muốn xóa danh mục này?</h3>
<div>
    <dl class="dl-horizontal col-md-4">
        <dt>
            @Html.LabelFor(model => model.TENDANHMUC, "Tên danh mục",htmlAttributes: new { @class = "bmd-label-floating" })
        </dt>

        <dd>
            @Html.DisplayFor(model => model.TENDANHMUC)
        </dd>
        <dt>
            @using (Html.BeginForm())
            {
                @Html.AntiForgeryToken()
              
                    <input type="submit" value="Lưu" class="btn btn-sm btn-primary " />
                    @Html.ActionLink("Quay lại", "Index", null, new { @class = "btn btn-sm btn-primary " })
                
            }
        </dt>
    </dl>
</div>
